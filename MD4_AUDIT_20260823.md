# MD(4) 구현 재감사 v2 — 2026-08-23

기준 문서: `자유민턴_관리자APK_사용자웹_사용자 APK(4).md`

이번 v2는 MD 184줄을 다시 처음부터 읽고, v1 감사에서 문장을 과하게 해석하거나 놓친 부분을 정정한 것이다.

## 판정 원칙
- 최신 MD(4)의 문장 뜻을 먼저 확정한 뒤 현재 `main`과 대조한다.
- 백엔드 함수만 있으면 PASS가 아니다. UI/APK/배포경로까지 실제 연결을 확인한다.
- 반대로 서버 한 부분만 보고 전체 기능을 FAIL 처리하지 않는다. 프론트에서 보완되는 로직도 함께 본다.
- 확인하지 않은 항목은 PASS로 쓰지 않는다.

## v1 감사에서 잘못 판정한 부분

### 1. 사용자 우선 처리 — v1의 해석이 과했음
MD의 `사용자가 자신을 대기나 코트에 배정한 경우를 먼저 우선시 하고 남은 부분은 관리자가 ... 채운다`는 문장은 네트워크 요청 우선순위 큐를 반드시 구현하라는 의미로 보기보다, 사용자가 이미 잡은 자리를 보존하고 관리자는 남은 자리만 채우라는 운영 규칙으로 해석하는 것이 맞다.

현재 Durable Object 직렬화와 이미 코트/대기에 들어간 멤버를 자동배정 후보에서 제외하는 구조는 이 취지에 부합할 수 있다. 따라서 v1의 `명시적 사용자 우선 큐 없음 = PARTIAL/FAIL` 판정은 취소한다. 최종 판정은 동시 변경 회귀테스트 후 한다.

### 2. 백업 저장 — v1 PASS가 잘못됨
MD는 `백업 저장(회원전체 입력상태 모두가 대기에 있는 상태 1개만)`이라고 적고 있다.
현재 서버는 기존 백업을 삭제해 1개만 유지하는 것은 맞지만, `현재 상태 그대로` JSON을 저장한다.
MD 문장대로라면 회원 입력정보는 보존하되 코트/대기1~5/제외상태가 아닌 `모두 코트배정 대기` 상태의 복원점을 1개 만드는 것이 요구사항에 더 가깝다.
따라서 `백업 1개 저장 = PASS`는 취소하고 `PARTIAL/FAIL`로 정정한다.

### 3. 자동배정 — v1의 전체 FAIL 판정이 과했음
현재 프론트 Script에는 실제로 복식 4명 조합을 `남4 / 여4 / 남2+여2`만 허용하는 `isAllowedDoublesTeam()` 로직이 있다. 또한 `EMPTY_SLOT_TARGETS`로 코트/대기 빈자리를 여러 개 연속 선택하는 구조도 존재한다.
따라서 state-worker의 단순 남/여 교차 코드만 보고 `자동배정 전체 FAIL`로 판정한 것은 잘못이다.

정정 판정:
- 정상 4명 복식 조합 검사: PASS 후보
- 빈자리 다중 선택: PASS 후보
- 마지막 잔여 인원이 복식 조합을 못 맞출 때 규칙을 풀어 최대한 배정: 확인 필요/현재 엄격 검사 때문에 실패 가능성 있음
- 서버 단독 autoAssign 경로와 프론트 조합 로직의 일관성: 보완 필요

## 이번 재점검에서 새로 발견한 누락/오류

### 4. 코트배정대기(active) 사람과 자리교환이 서버에서 막힐 수 있음
MD는 상대방 카드가 `코트내 / 대기1~5 / 코트배정대기` 어디에 있어도 자리교환 요청이 가능해야 한다.
현재 `locationOf()`는 코트와 대기1~5만 위치로 반환하고 코트배정대기(active)는 `null`을 반환한다.
`requestSwapMutation()`은 requester/target 둘 다 `locationOf()`가 있어야 하므로 코트배정대기 사람과 교환 요청이 실패할 수 있다.
이 항목은 새 확정 FAIL이다.

### 5. 웹 포그라운드 진동 반복 횟수가 MD와 다름
MD는 대기1, 코트입장 모두 `3회 진동 × 8회`를 요구한다.
현재 Script의 `memberVibrationPattern()`은 3번 진동 묶음 자체는 맞지만 `memberAlertRepeatCount()`가 `court_assignment=4`, `wait1_ready=2`로 되어 있다.
따라서 앱 미설치 웹 사용자의 현재 포그라운드 진동은 MD와 불일치한다.
FCM Worker에 repeatCount=8이 있다고 웹 동작까지 PASS 처리하면 안 된다.

### 6. 관리·설정 메뉴의 구성 자체가 MD와 다름
MD는 관리·설정 버튼을 열면 4개: `경기통계 / 백업 저장 / 백업복원 / 전체초기화`를 요구한다.
현재 Admin.html에는 `음성 안내 켜짐 / 음성 테스트 / 백업 저장 / 백업 복원 / 전체 초기화`가 들어 있고 `경기통계`가 없다.
음성 조작은 코트 운영 영역의 재생/반복/멈춤에서 처리할 수 있으므로 관리·설정 4개 구성과 정확히 맞추는 것이 필요하다.

### 7. 경기통계는 단순 게임횟수만으로 부족
MD는 `총 게임횟수와 누구와 게임을 했는지`까지 요구한다.
현재 state 모델은 member.games는 있으나 경기별 함께 뛴 상대 이력을 보존하는 구조가 확인되지 않는다.
경기 종료/코트 입장 시 4명 단위 경기 이력을 기록해야 경기통계를 제대로 만들 수 있다.

## 계속 유효한 주요 FAIL/PARTIAL

### 운영 구조
- 관리자 APK가 아직 `script.google.com` Apps Script deployment를 실제 빌드 경로에서 사용: FAIL
- 사용자 APK도 Apps Script URL을 사용: FAIL
- 사용자 설치 버튼 배포도 clasp pull/push 사용: FAIL
- Cloudflare state-worker/D1/DO 코드 자체는 존재: 서버 기반 PASS

### 사용자 APK/알림
- user-push FCM Worker는 존재
- 현재 실제 사용자 APK 빌드에는 Firebase Messaging 수신 서비스/token 등록 구조가 없음: FAIL
- 관리자 APK에서 본인을 사용자로 설정했을 때 동일 알림을 받는 FCM 구조도 없음: FAIL

### 관리자 멤버등록
- 총인원/남/여 간략 표시: 현재 MD 형태와 불일치
- 성별: MD는 라디오식, 현재 select: FAIL
- 급수/구력: MD는 한꺼번에 쓰는 곳, 현재 별도 입력: FAIL
- 메모 입력: 누락
- 신규 체크: 누락
- 찬조 체크: 누락
- 신규/찬조 카드 표시 연결: 누락
- 게임횟수 +1 버튼: 누락

### 제외인원/빠른배정
- `코트배정 제외`는 빠른 코트배정 쪽에 항상 보여야 하나 현재 접히는 관리자 설정 내부: FAIL
- 긴누름 메뉴는 `배정대기로/도착전/휴식/귀가/삭제/취소`여야 하나 현재 구성과 다름: FAIL

### 코트/타이머
- 방향선택 UI와 4-3-2-1 / 1-2-3-4 구조: PASS 후보
- 서버 moveMutation은 첫 멤버가 코트에 들어갈 때 courtStartedAt을 시작함: MD와 일치
- 다만 프론트 일부 로컬 상태 코드가 4명일 때만 시간을 세팅/유지하는 코드가 있어 서버와 불일치 가능: 수정 필요
- 다음 종료 예상 1위 구조: PASS 후보
- 코트 진입 시 게임수 +1 서버 로직: PASS 후보

### 경기 종료
- 0명 코트도 종료 가능: PASS 서버
- 대기1→코트, 대기2→1, 3→2, 4→3, 5→4: PASS 서버
- 1회 재생/반복/멈춤 UI: 존재
- 음악 완전 mute 금지/duck 후 복원: 코드 존재, 단말 검증 필요
- 여자 음성·최대볼륨·정확히 3회 기본 반복은 실제 단말/함수 재검증 필요

### 하단 버튼
MD는 맨 아래에 `실행취소 / 새로고침 / 자동배정`이 꼭 있어야 한다.
현재 실행취소/자동배정은 있으나 관리자 맨 아래 새로고침 구성은 MD와 정확히 일치하지 않는다.

## 수정 우선순위 v2
1. GAS 운영 의존 제거 및 관리자/사용자 APK Cloudflare 직결
2. 사용자 APK 실제 FCM 수신/token 등록 + 관리자 본인 사용자 FCM
3. 코트배정대기(active)를 포함한 위치 모델 재정의 및 자리교환 수정
4. 웹/앱 3×8 진동 규칙 통일
5. 백업을 `회원 입력정보 보존 + 모두 코트배정대기` 복원점 1개로 수정
6. 관리자 관리·설정 4개 정확히 구성 + 경기통계 경기상대 이력 추가
7. 신규/찬조/메모/성별 라디오/급수·구력 통합/+1/남녀 인원표시 수정
8. 제외인원 위치와 긴누름 메뉴 수정
9. 자동배정 마지막 잔여인원 예외 및 서버/프론트 로직 일치
10. 타이머/음성/하단 버튼 전수 회귀검증

## 완료 판정 금지 항목
실제 APK 단말과 Cloudflare 운영 주소에서 검증하기 전에는 다음을 PASS로 확정하지 않는다.
- FCM 수신
- 화면 꺼짐/백그라운드 진동
- 확인 버튼 즉시 진동 중지
- 여자 TTS 선택
- 음악 duck/restore
- 동시 사용자/관리자 자리변경
- 로그인 자동 재진입
