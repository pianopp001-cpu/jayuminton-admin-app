# 자유민턴 v1.5 한 페이지 통합 사용자 화면

최종 사용자 주소는 `https://jayuminton-push.web.app/` 하나입니다. 이 주소 안에 기존 Apps Script 사용자 코트 현황을 넣고, 같은 화면에서 이름 선택과 FCM 웹 알림 등록을 처리합니다.

## 동작

- 멤버 APK를 설치하지 않습니다.
- 별도의 알림 전용 페이지를 사용자에게 노출하지 않습니다.
- 알림을 누르면 열려 있는 자유민턴 화면을 앞으로 가져오고 현황 데이터만 즉시 갱신합니다.
- 화면이 닫혀 있으면 같은 통합 주소를 열고 바로 갱신합니다.
- 로그인 버전과 선택한 이름을 통합 화면에 저장합니다.
- iPhone 홈 화면 웹앱 설치 주소에 로그인 버전과 선택 이름을 이어 줍니다.
- Android 카카오톡·당근 내장 브라우저에서는 Chrome 연결을 먼저 시도하고, 차단될 때만 한 번 누르는 버튼을 표시합니다.
- 코트 시간은 5분 이하 기본, 5~10분 녹색, 10~15분 주황, 15분 초과 빨강으로 표시합니다.
- 가장 오래 진행된 코트에 `다음 종료 예상 1순위`를 표시합니다.

## 배포

1. 별도 제공되는 `apps-script-main`의 `Index.html`, `Script.html`, `Style.html`, `Admin.html`을 기존 코트배정 Apps Script 프로젝트에 각각 전체 교체하고 새 버전으로 배포합니다.
2. `apps-script-push/Code.gs`와 `appsscript.json`을 알림 발송 Apps Script에 반영하고 새 버전으로 배포합니다.
3. `deploy-web-push.bat`를 실행하고 알림 발송 Apps Script의 `/exec` 주소를 입력합니다.
4. 앞으로 멤버에게는 `https://jayuminton-push.web.app/`만 공유합니다.

실기기 확인 전에는 이 브랜치를 `main`에 병합하지 않습니다.
