# 자유민턴 추가 요구사항 — 2026-08-24

기준 문서: 사용자 업로드 `관리자_사용자.md`

## 1. 관리자 저장중 잠금
- 관리자에서 서버 저장/배정/상태변경 처리 중에는 화면에 `저장중` 상태를 명확하게 표시한다.
- 저장중에는 관리자 화면의 일반 조작 버튼/카드/배정/종료/설정 입력을 눌러 중복 요청을 발생시킬 수 없도록 막는다.
- 단, **음성제어 영역은 저장중 오버레이 위에서도 항상 조작 가능**해야 한다.
- 저장중에도 허용되는 음성제어: 음소거, 1회 재생, 반복재생, 멈춤 등 현재 음성 멘트 제어 전부.
- 구현 시 전체 화면을 무조건 `pointer-events:none` 처리하지 말고, 음성제어 영역만 예외로 활성화되도록 한다.

## 2. 사용자 자기카드 긴누름 메뉴
사용자 웹과 사용자 APK에서 본인이 설정한 자기 카드만 길게 누르면 다음 버튼을 표시한다.
- 도착전
- 코트배정대기
- 휴식
- 귀가
- 내정보입력

타인의 카드에는 이 자기상태 변경 메뉴를 노출하지 않는다.

## 3. 사용자 내정보입력 — 공개 메모
- `내정보입력`을 누르면 사용자가 자신의 공개 메모를 입력/수정할 수 있다.
- 메모는 카드의 `구력` 정보와 같은 수준의 **아주 작은 보조 글씨 크기**로 이름 아래 정보 영역에 표시한다.
- 메모가 비어 있으면 빈 문구/`입력 없음` 등을 출력하지 않는다.
- 사용자가 입력한 공개 메모는 서버 상태에 저장되어 웹 새로고침/APK 재접속 후에도 유지한다.

## 4. 관리자 카드에 사용자 공개 메모 실시간 반영
- 사용자가 웹/APK의 `내정보입력`에서 변경한 공개 메모를 관리자 회원카드에도 동일하게 표시한다.
- 관리자 카드에서도 급수/구력 등 작은 보조정보와 같은 수준으로 작게 표시한다.
- 사용자가 메모를 수정/삭제하면 관리자 카드에도 다음 상태 갱신 시 동일하게 수정/삭제되어야 한다.
- 관리자 저장 작업과 사용자 메모 갱신이 겹칠 경우 Durable Object 직렬화/최신 서버 상태를 기준으로 충돌 없이 반영한다.

## 검증 조건
1. 저장 시작 즉시 `저장중` 표시가 보인다.
2. 저장중 일반 버튼을 연속 터치해도 추가 동작이 발생하지 않는다.
3. 저장중 음성이 재생 중이면 `멈춤`이 즉시 동작한다.
4. 저장중에도 음소거/재생/반복 제어가 가능하다.
5. 사용자 자기카드 긴누름에 정확히 5개 메뉴가 나온다.
6. `내정보입력`에서 작성한 메모가 자기 카드에 작은 글씨로 나온다.
7. 같은 메모가 관리자 카드에도 반영된다.
8. 메모 삭제 시 양쪽 카드에서 사라진다.
9. 재접속/새로고침 이후에도 저장된 메모가 유지된다.
