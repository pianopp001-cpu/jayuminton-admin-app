name: Inspect Jayuminton push source - manual only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Manual inspection notice
        run: |
          echo 'Automatic push inspection is intentionally disabled after the verified live deployment.'
          echo 'The guarded live workflow performs syntax, native-PWA-only, token-registration, vibration, and live-byte checks before reporting success.'
