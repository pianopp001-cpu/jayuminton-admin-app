name: DISABLED legacy v200.4 fresh-install APK build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Block obsolete v200.4 build
        run: |
          echo 'DISABLED: this workflow builds the obsolete v200.4 admin APK and must not replace the recovered v203 Cloudflare APK.'
          echo 'Use the verified v203 Cloudflare admin APK build path.'
          exit 1
