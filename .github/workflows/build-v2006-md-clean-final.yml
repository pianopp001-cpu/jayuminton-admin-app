name: DISABLED legacy admin v201.4 APK build

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy regression path
        shell: bash
        run: |
          echo 'DISABLED: legacy v201.4 admin APK build must never replace canonical v203.0.' >&2
          echo 'Use the canonical v203.0 admin APK workflow pinned in PROJECT_RULES.md and deployment/status/production-matrix-v203-v1642.txt.' >&2
          exit 1
