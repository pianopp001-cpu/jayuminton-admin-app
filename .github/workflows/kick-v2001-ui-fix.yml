name: Legacy v200.1 UI rebuild (manual only)

# Superseded by admin-redeploy-build-chain.yml + build-v1995-direct.yml (v200.4).
# Kept only for historical/manual recovery. Never runs automatically on main pushes.
on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: |
          echo 'v200.1 automatic orchestrator is disabled.'
          echo 'Use admin-redeploy-build-chain.yml and build-v1995-direct.yml for current admin deployment/APK.'
