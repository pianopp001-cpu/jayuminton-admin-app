name: Apply member wait-seat swap phase 2 (manual legacy)

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy workflow disabled from automatic execution. Current Cloudflare admin flow owns move/swap.'
