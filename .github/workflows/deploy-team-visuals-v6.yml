name: Deploy final team visuals v6 (legacy manual only)

on:
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: jayuminton-unified-member-production
  cancel-in-progress: false

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy duplicate auto-deploy disabled; use deploy-member-single-popup-v7.yml.'
