name: Legacy Jayuminton PWA deploy disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Disabled legacy workflow."
          echo "Use .github/workflows/continue-exact-v1637-restore.yml only."
