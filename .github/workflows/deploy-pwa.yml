name: Deploy Jayuminton

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    name: Legacy deploy paused
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy v1.6.41 deploy is paused while the exact v1.6.37 design source is restored.'
