name: Disabled legacy v1.2.8 installer deploy

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy v1.2.8 live installer deployment is disabled. Use the v1.2.9 deployment workflow.'
