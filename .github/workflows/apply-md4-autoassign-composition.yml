name: Apply MD4 autoassign composition (manual legacy)

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy MD4 autoassign patch parked. Current Cloudflare admin source is authoritative.'
