name: DISABLED legacy MD4 core rollback patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Block obsolete MD4 core contract
        run: |
          echo 'DISABLED: this workflow rewrites current Cloudflare/source snapshot behavior with an obsolete MD4 contract, including old vibration repetition.'
          echo 'Use the latest v203/MD7 Cloudflare baseline instead.'
          exit 1
