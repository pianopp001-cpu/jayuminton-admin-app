name: Repair user wait1 push live (manual legacy)

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy GAS-era wait1 diagnostic disabled from automatic execution.'
