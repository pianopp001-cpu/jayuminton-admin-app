name: DISABLED legacy lightweight GAS user APK v1.4.1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Block obsolete GAS/lightweight user APK path
        run: |
          echo 'DISABLED: this workflow resolves MAIN_DEPLOYMENT_ID and builds the obsolete lightweight/GAS user APK lineage.'
          echo 'Production user APK is Cloudflare-only v1.6.42 with native FCM and 3-pulses x 8 groups.'
          exit 1
