name: Apply MD5 Cloudflare pair statistics

on:
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: legacy-md5-pair-statistics
  cancel-in-progress: true

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy MD5 repair is manual-only; current Cloudflare admin source is authoritative."
