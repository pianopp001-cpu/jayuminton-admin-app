name: Record direct v199.5 APK build result

on:
  workflow_run:
    workflows: ['Build Jayuminton v199.5 direct APK']
    types: [completed]

permissions:
  contents: write

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Record result
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p deployment/status
          cat > deployment/status/v1995-direct-apk-build.txt <<EOF
          workflow=Build Jayuminton v199.5 direct APK
          conclusion=${{ github.event.workflow_run.conclusion }}
          run_id=${{ github.event.workflow_run.id }}
          head_sha=${{ github.event.workflow_run.head_sha }}
          updated_at=${{ github.event.workflow_run.updated_at }}
          EOF
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add deployment/status/v1995-direct-apk-build.txt
          git commit -m 'Record direct v199.5 APK result: ${{ github.event.workflow_run.conclusion }}' || exit 0
          git pull --rebase origin main || true
          git push origin main
