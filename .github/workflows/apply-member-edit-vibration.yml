name: DISABLED legacy GAS member edit/vibration patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy GAS/clasp patch
        run: |
          echo 'DISABLED: this workflow pulls and patches Google Apps Script with clasp.'
          echo 'The production baseline is Cloudflare v203; do not run GAS restore/patch workflows against it.'
          exit 1
