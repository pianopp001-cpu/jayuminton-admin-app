name: Legacy v1.6.37 restore disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled legacy workflow
        run: |
          echo 'This legacy restore is intentionally disabled.'
          echo 'It contained a stale Apps Script deployment ID and could regress the current admin/PWA state.'
          echo 'Use the dedicated stable admin and verified push deployment workflows instead.'
