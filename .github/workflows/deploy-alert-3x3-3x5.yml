name: Archived - user alerts 3x3 3x5 already deployed

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Archived after successful 3x3/3x5 deployment.'
          echo 'Do not recreate browser validation files inside the Apps Script project.'
          echo 'Use a dedicated safe deployment workflow for future alert changes.'
