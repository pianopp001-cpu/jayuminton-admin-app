name: Diagnose v200.4 preview markers (manual legacy)

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy v200.4 preview diagnostic disabled from automatic execution.'
