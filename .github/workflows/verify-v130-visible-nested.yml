name: Verify v1.3.0 nested court page visible (manual legacy)

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy GAS iframe verifier disabled. Current deployment is Cloudflare-based.'
