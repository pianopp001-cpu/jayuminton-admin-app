# DEPRECATED. GAS-backed admin deployment is intentionally disabled.
# The project contract requires Cloudflare Worker/D1 only.
name: Deprecated admin GAS deploy (disabled)

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Do not use Google Apps Script
        run: |
          echo 'GAS_DEPLOY_DISABLED_USE_CLOUDFLARE_ONLY_WORKFLOW'
          echo 'Use deploy-cloudflare-admin-save-lock-memo.yml instead.'
