name: Retired MD6 self longpress production

on:
  workflow_dispatch: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. User web keeps the original self-card long-press popup; memo/profile addon deployment is disabled."
