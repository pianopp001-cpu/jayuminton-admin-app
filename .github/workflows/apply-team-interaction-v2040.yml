name: Apply team interaction v2040 (disabled obsolete)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled: obsolete workflow restored Team1/Team2 labels and conflicts with shared temp-pair v3.'
