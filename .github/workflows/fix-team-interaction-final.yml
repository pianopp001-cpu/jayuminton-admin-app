name: Fix final team interaction (manual legacy)

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy team workflow disabled. Current v2046 multi-select interaction is authoritative.'
