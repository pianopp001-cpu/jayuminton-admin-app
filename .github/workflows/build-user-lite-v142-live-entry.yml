name: DISABLED legacy lightweight user APK v1.4.2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Block obsolete lightweight user APK lineage
        run: |
          echo 'DISABLED: this workflow builds the obsolete lightweight v1.4.2 package and removes native Firebase messaging.'
          echo 'Production user APK is Cloudflare-only v1.6.42 with com.jayuminton.user and native FCM.'
          exit 1
