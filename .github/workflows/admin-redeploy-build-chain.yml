name: Redeploy admin Cloudflare and rebuild APK

on:
  push:
    branches: [main]
    paths:
      - 'deployment/jayuminton/apply_admin_vnext_patches.py'
      - 'deployment/jayuminton/v3_admin_overlay_guarantee.py'
      - 'deployment/jayuminton/admin_vnext_*.py'
      - 'deployment/jayuminton/admin_finish_alert_v12_patch.py'
      - 'deployment/jayuminton/admin_cloudflare_final_contract.py'
      - 'deployment/jayuminton/admin_cloudflare_post_contract_patch.py'
      - '.github/workflows/admin-redeploy-build-chain.yml'
  workflow_dispatch: {}

permissions:
  actions: write
  contents: read

jobs:
  redeploy-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Redeploy Cloudflare admin and wait
        shell: bash
        run: |
          set -euo pipefail
          old="$(gh run list --repo "$GITHUB_REPOSITORY" --workflow preview-admin-cloudflare.yml --branch main --event workflow_dispatch --limit 1 --json databaseId --jq '.[0].databaseId // 0')"
          gh workflow run preview-admin-cloudflare.yml --repo "$GITHUB_REPOSITORY" --ref main
          run_id=''
          for i in $(seq 1 60); do
            run_id="$(gh run list --repo "$GITHUB_REPOSITORY" --workflow preview-admin-cloudflare.yml --branch main --event workflow_dispatch --limit 1 --json databaseId --jq '.[0].databaseId // 0')"
            if [ "$run_id" != "0" ] && [ "$run_id" != "$old" ]; then break; fi
            sleep 2
          done
          test -n "$run_id"; test "$run_id" != "0"; test "$run_id" != "$old"
          gh run watch "$run_id" --repo "$GITHUB_REPOSITORY" --exit-status

      - name: Build latest verified admin v203.0 APK and wait
        shell: bash
        run: |
          set -euo pipefail
          old="$(gh run list --repo "$GITHUB_REPOSITORY" --workflow build-v2015-known-good-shell.yml --branch main --event workflow_dispatch --limit 1 --json databaseId --jq '.[0].databaseId // 0')"
          gh workflow run build-v2015-known-good-shell.yml --repo "$GITHUB_REPOSITORY" --ref main
          run_id=''
          for i in $(seq 1 60); do
            run_id="$(gh run list --repo "$GITHUB_REPOSITORY" --workflow build-v2015-known-good-shell.yml --branch main --event workflow_dispatch --limit 1 --json databaseId --jq '.[0].databaseId // 0')"
            if [ "$run_id" != "0" ] && [ "$run_id" != "$old" ]; then break; fi
            sleep 2
          done
          test -n "$run_id"; test "$run_id" != "0"; test "$run_id" != "$old"
          gh run watch "$run_id" --repo "$GITHUB_REPOSITORY" --exit-status

      - name: Chain completed
        run: echo 'ADMIN_REDEPLOY_AND_LATEST_V2030_APK_BUILD_OK'

# force-trigger: 2026-08-24-v203-public-memo-save-lock
