name: Build unified MD-final user APK v1.6.42

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/build-user-md-final-v1642.yml'
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: jayuminton-user-md-final-v1642
  cancel-in-progress: true

jobs:
  drain-stale-run:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'stale user APK run drained; canonical workflow restored next commit'
